import { Table } from "../components/Table/Table"
import main from './main.module.sass'

export const Main = () => {

    return (
        <main className={main.main}>
        <Table />
        </main>
    )
}